// The frontend entrypoint. Vite bundles this (and its imports) into
// public/assets/, and the `Vite` service tags it into your HTML.
import "../css/app.css";

console.log("⚓ Keel + Vite — hot module reload is live.");

// Editing this file (or app.css) updates the browser instantly while
// `npm run dev:client` is running, no full reload.
