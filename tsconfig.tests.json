{
  // Type-checks the test suite. Tests run through tsx, which strips types without
  // checking them, so without this a test could assert a type at compile time and
  // be asserting nothing at all.
  "extends": "./tsconfig.json",
  "compilerOptions": { "noEmit": true },
  "include": ["tests", "src"]
}
